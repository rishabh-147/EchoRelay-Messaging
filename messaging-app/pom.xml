<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.5</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>net.echorelay</groupId>
	<artifactId>messaging-app</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>EchoRelay</name>
	<description>Messaging API using Kafka and Spring Boot This project is a real-time messaging API built using Spring Boot, Kafka, and PostgreSQL. It facilitates efficient message exchange between users by leveraging Kafka’s distributed streaming architecture for high-throughput event-driven communication.  Key Features: Kafka-based Messaging: Ensures scalable and fault-tolerant message processing.  Spring Boot Framework: Provides a clean and modular architecture for API development.  PostgreSQL Integration: Enables structured storage of messages with efficient indexing.  RESTful API Endpoints: Supports message sending and retrieval with real-time updates.  Producer &amp; Consumer Implementation: Uses Kafka producers for message publishing and consumers for retrieval.  Scalability &amp; Reliability: Optimized for handling large volumes of messages seamlessly.  This project serves as a learning foundation for understanding Kafka’s role in real-time messaging and its integration with modern web applications. 🚀</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
	</properties>
	<dependencies>
    <!-- Spring Boot & Web -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
    	<groupId>org.springframework.boot</groupId>
    	<artifactId>spring-boot-starter-test</artifactId>
    	<scope>test</scope>
	</dependency>

    <!-- Kafka -->
		<dependency>
			<groupId>org.springframework.kafka</groupId>
			<artifactId>spring-kafka</artifactId>
			<version>3.3.3</version>
		</dependency>

		<!-- PostgreSQL -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
   <dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <version>42.7.3</version> <!-- use latest -->
</dependency>


		<!-- JSON Processing -->
    <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
    </dependency>

		<!-- Validation -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    
    
</dependencies>


	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
